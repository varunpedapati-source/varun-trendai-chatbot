apiVersion: v1
kind: Secret
metadata:
  name: chatbot-secrets
  namespace: chatbot
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_ME"
  MONGODB_URI: "mongodb://root:REPLACE_ME@mongo.mongodb.svc.cluster.local:27017/chatbot?authSource=admin"

